.next/
node_modules/
.env.local
.env*.local
*.log
.DS_Store
.vercel
*.tmp
test-*.mjs
.sfdx/

# Standalone sub-project (separate repo, not deployed with marketplace)
apollo-lead-engine/

# Runtime state files (regenerated at runtime)
data/ratelimit.json
